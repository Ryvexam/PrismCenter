name: Apply hyperscale UI migration

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled migration
        run: echo "Self-mutating migration disabled. Apply UI changes in a normal commit instead."
